算法思路：
建立好的kd-tree中只有叶子节点存放数据，其余根节点和双亲节点均存放划分标准
建立：
1.将当前节点中存储的数据集具有最大方差的维度作为当前分裂节点划分标准
2.将当前所选择的分类维度中的中位数m作为具体的分裂标准(d<=m分到左子树，大于m分到右子树)
搜索
1.将待搜索点按照建立好的kd-tree逐层搜索到叶子结点，而后在此叶子节点中根据最近邻半径找到最近邻，而后回溯
2.在待搜索点逐层到达叶子节点的过程中把经历到的中间双亲节点对应另一个未被遍历的孩子节点存放到顺序数组中存储，回溯时从顺序数组中逐个出队回溯，直到到达最大回溯值后停止回溯。
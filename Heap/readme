知识点：
1.堆结构的性质：完全二叉树，父节点和左右子节点索引的关系，注意所以关系表达式中的索引从1开始计数；
2.维护堆结构的性质：递归维护函数
3.建立堆结构：从后往前逐个维护堆结构，从low_int(logn)+1到length_size均为叶子结点，所以从low_int(logn)开始往前建立；
不能从前往后建立，因为后边的节点值如果大于前边的堆顶，则无法返回重新维护；
4.堆排序：利用堆的性质，依次将堆顶元素与堆底元素交换，并在交换后从堆顶开始递归维护。